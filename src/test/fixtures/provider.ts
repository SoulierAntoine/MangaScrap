// const Node = require('../../main/models/node');

import Node from '../../main/models/node';

/* const tree = new Node(1, -1, [
	new Node(2, 1, Array.of(new Node(5, 2, [], 3)), 2),
	new Node(3, 1, Array.of(
		new Node(6, 3, [], 3),
		new Node(7, 3, [], 3),
	), 2),
	new Node(4, 1, [], 2)
], 1); */

// module.exports.tree = tree;
