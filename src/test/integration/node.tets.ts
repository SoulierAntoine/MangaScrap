// const {tree} = require('../fixtures/provider');
// import NodeUtils from '../../main/commons/utils/node-utils';
// import Node from '../../main/models/node';


describe('node', () => {
	test('get nodes at level', () => {
		// const childrenAtLevel: Node[] = NodeUtils.getNodesAtLevel(tree, 3);
		// expect(childrenAtLevel.map((child: Node) => child.id)).toEqual([5,6,7]);
	});
});
