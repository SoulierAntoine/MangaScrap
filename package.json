{
  "name": "manga-scrap",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.js",
  "keywords": [],
  "author": "altan",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^8.2.0",
    "puppeteer": "^8.0.0"
  },
  "scripts": {
    "prebuild": "rimraf ./build",
    "build": "tsc -p conf/tsconfig.json",
    "postbuild": "copyfiles --soft --flat src/main/conf/* ./build/dist/conf",
    "start": "cross-env node build/dist/index.js",
    "run": "npm run build && npm run start",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "copyfiles": "^2.4.1",
    "jest": "^26.0.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.0.0",
    "tslint": "^6.1.3",
    "typescript": "^4.2.3"
  }
}
